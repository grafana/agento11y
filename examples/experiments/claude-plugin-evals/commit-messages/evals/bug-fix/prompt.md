---
max_turns: 10
allowed_tools: [Read, Glob, Grep, Skill]
---

Write me a commit message for this change: I fixed the checkout total so that a discount is applied only once, even when the user retries payment.
