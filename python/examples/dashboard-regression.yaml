suite_id: dashboard-regression
name: Dashboard regression
version: 1.0.0
description: Regression suite for the dashboarding agent.
tags:
  - dashboards
  - regression
cases:
  - id: add-cache-lag-panels
    name: Add cache lag panels
    input: Add two panels showing cache read and write lag to the Cache Health dashboard.
    expected: A dashboard with two new timeseries panels for cache read and write lag.
    tags:
      - panels
  - id: add-deployment-annotation
    name: Add deployment annotation
    input: Add a deployment annotation layer to the API Latency dashboard.
    expected: A dashboard with a deployment annotation query attached.
    tags:
      - annotations
